# Latency tests

Perf harness measuring timing characteristics against a Nova Sonic test stream,
marked `@pytest.mark.latency`: response-start <= 1.5 s (12.1) and barge-in stop
<= 500 ms (12.2). Measured, not property-tested.
