# Unit tests

Example / edge-case tests covering concrete happy paths and clarifying-question
behaviors (e.g. successful config save, empty-config responses, single-answer
FAQ, cancel confirmation prompt). Unit tests are unmarked.
