# Smoke tests

Single-execution one-time configuration/architecture checks, marked
`@pytest.mark.smoke`: scheduler interval <= 24 h (13.1) and the per-entity
interface existence / agents-only-use-interfaces architectural check (16.1).
