"""Dashboard backend-for-frontend and web components.

Populated across tasks 12.x/13.x: the BFF that reads exclusively through the
Data_Layer with ChangeEvent fan-out, decision approve/dismiss execution,
``RoleGate`` access control, impact metrics, the call-activity log, and the
web components (``DecisionsFeed``, ``ScheduleView``, ``CallActivityLog``,
``ImpactMetricsStrip``, ``OnboardingWizard``).
"""
